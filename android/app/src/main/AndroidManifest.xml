<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Alarm / scheduling -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD"/>
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <!-- Scanner + feedback -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <uses-feature android:name="android.hardware.camera" android:required="true"/>

    <application
        android:label="Fajarly Pro"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        android:showWhenLocked="true"
        android:turnScreenOn="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:showOnLockScreen="true">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- OEM permission-editor intent actions -->
        <intent>
            <action android:name="miui.intent.action.APP_PERM_EDITOR"/>
        </intent>
        <intent>
            <action android:name="miui.intent.action.APP_PERMISSIONS_EDITOR"/>
        </intent>
        <!-- OEM permission-editor + autostart packages (Android 11+
             package visibility requires explicit declaration) -->
        <package android:name="com.oplus.safecenter"/>              <!-- OxygenOS 13+ / ColorOS 13+ -->
        <package android:name="com.coloros.safecenter"/>            <!-- OxygenOS/ColorOS <13 -->
        <package android:name="com.oppo.safe"/>                     <!-- legacy Oppo -->
        <package android:name="com.miui.securitycenter"/>           <!-- MIUI / HyperOS -->
        <package android:name="com.vivo.permissionmanager"/>        <!-- FuntouchOS -->
        <package android:name="com.iqoo.secure"/>                   <!-- OriginOS -->
        <package android:name="com.huawei.systemmanager"/>          <!-- Magic OS / EMUI -->
        <package android:name="com.samsung.android.lool"/>          <!-- One UI -->
        <package android:name="com.asus.mobilemanager"/>            <!-- ZenUI -->
    </queries>
</manifest>
